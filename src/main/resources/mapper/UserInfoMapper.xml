<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.clazs.easymeeting.mapper.UserInfoMapper">
    <!--实体映射-->
    <resultMap id="base_result_map" type="cn.clazs.easymeeting.entity.po.UserInfo">
        <!-- 用户ID -->
        <id column="user_id" property="userId"/>
        <!-- 邮箱 -->
        <result column="email" property="email"/>
        <!-- 昵称 -->
        <result column="nick_name" property="nickName"/>
        <!-- 性别 0：女 1：男 2：保密 -->
        <result column="sex" property="sex"/>
        <!-- 密码 -->
        <result column="password" property="password"/>
        <!-- 状态 -->
        <result column="status" property="status"/>
        <!-- 创建时间 -->
        <result column="create_time" property="createTime"/>
        <!-- 最后登录时间 -->
        <result column="last_login_time" property="lastLoginTime"/>
        <!-- 最后离开时间 -->
        <result column="last_off_time" property="lastOffTime"/>
        <!-- 个人会议号 -->
        <result column="meeting_no" property="meetingNo"/>
    </resultMap>

    <!--通用查询结果列-->
    <sql id="base_column_list">
        user_id,email,nick_name,sex,password,status,create_time,last_login_time,last_off_time,meeting_no
    </sql>

    <!--基础查询条件-->
    <sql id="base_query_condition">
        <if test="query.userId != null and query.userId!=''">
            and user_id = #{query.userId}
        </if>
        <if test="query.email != null and query.email!=''">
            and email = #{query.email}
        </if>
        <if test="query.nickName != null and query.nickName!=''">
            and nick_name = #{query.nickName}
        </if>
        <if test="query.sex != null">
            and sex = #{query.sex}
        </if>
        <if test="query.password != null and query.password!=''">
            and password = #{query.password}
        </if>
        <if test="query.status != null">
            and status = #{query.status}
        </if>
        <if test="query.createTime != null">
            and create_time = #{query.createTime}
        </if>
        <if test="query.lastLoginTime != null">
            and last_login_time = #{query.lastLoginTime}
        </if>
        <if test="query.lastOffTime != null">
            and last_off_time = #{query.lastOffTime}
        </if>
        <if test="query.meetingNo != null and query.meetingNo!=''">
            and meeting_no = #{query.meetingNo}
        </if>
    </sql>

    <!--扩展的查询条件-->
    <sql id="base_query_condition_extend">
        <if test="query.userIdFuzzy != null and query.userIdFuzzy !=''">
            and user_id like concat('%', #{query.userIdFuzzy}, '%')
        </if>
        <if test="query.emailFuzzy != null and query.emailFuzzy !=''">
            and email like concat('%', #{query.emailFuzzy}, '%')
        </if>
        <if test="query.nickNameFuzzy != null and query.nickNameFuzzy !=''">
            and nick_name like concat('%', #{query.nickNameFuzzy}, '%')
        </if>
        <if test="query.passwordFuzzy != null and query.passwordFuzzy !=''">
            and password like concat('%', #{query.passwordFuzzy}, '%')
        </if>
        <if test="query.createTimeStart != null and query.createTimeStart !=''">
            <![CDATA[ and create_time >= str_to_date(#{query.createTimeStart}, '%Y-%m-%d') ]]>
        </if>
        <if test="query.createTimeEnd != null and query.createTimeEnd !=''">
            <![CDATA[ and create_time < date_sub(str_to_date(#{query.createTimeEnd},'%Y-%m-%d'), interval -1 day) ]]>
        </if>
        <if test="query.meetingNoFuzzy != null and query.meetingNoFuzzy !=''">
            and meeting_no like concat('%', #{query.meetingNoFuzzy}, '%')
        </if>
    </sql>

    <!--通用查询条件-->
    <sql id="query_condition">
        <where>
            <include refid="base_query_condition"/>
            <include refid="base_query_condition_extend"/>
        </where>
    </sql>

    <!--查询列表-->
    <select id="selectList" resultMap="base_result_map">
        SELECT <include refid="base_column_list"/> FROM user_info <include refid="query_condition"/>
        <if test="query.orderBy != null"> order by ${query.orderBy}</if>
        <if test="query.simplePage != null"> limit #{query.simplePage.start}, #{query.simplePage.end}</if>
    </select>

    <!--查询数量-->
    <select id="selectCount" resultType="java.lang.Integer">
        SELECT count(1) FROM user_info <include refid="query_condition"/>
    </select>

    <!--插入（匹配有值的字段）-->
    <insert id="insert" parameterType="cn.clazs.easymeeting.entity.po.UserInfo">
        INSERT INTO user_info
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="bean.userId != null">
                user_id,
            </if>
            <if test="bean.email != null">
                email,
            </if>
            <if test="bean.nickName != null">
                nick_name,
            </if>
            <if test="bean.sex != null">
                sex,
            </if>
            <if test="bean.password != null">
                password,
            </if>
            <if test="bean.status != null">
                status,
            </if>
            <if test="bean.createTime != null">
                create_time,
            </if>
            <if test="bean.lastLoginTime != null">
                last_login_time,
            </if>
            <if test="bean.lastOffTime != null">
                last_off_time,
            </if>
            <if test="bean.meetingNo != null">
                meeting_no,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="bean.userId != null">
                #{bean.userId},
            </if>
            <if test="bean.email != null">
                #{bean.email},
            </if>
            <if test="bean.nickName != null">
                #{bean.nickName},
            </if>
            <if test="bean.sex != null">
                #{bean.sex},
            </if>
            <if test="bean.password != null">
                #{bean.password},
            </if>
            <if test="bean.status != null">
                #{bean.status},
            </if>
            <if test="bean.createTime != null">
                #{bean.createTime},
            </if>
            <if test="bean.lastLoginTime != null">
                #{bean.lastLoginTime},
            </if>
            <if test="bean.lastOffTime != null">
                #{bean.lastOffTime},
            </if>
            <if test="bean.meetingNo != null">
                #{bean.meetingNo},
            </if>
        </trim>
    </insert>

    <!--插入或者更新（匹配有值的字段）-->
    <insert id="insertOrUpdate" parameterType="cn.clazs.easymeeting.entity.po.UserInfo">
        INSERT INTO user_info
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="bean.userId != null">
                user_id,
            </if>
            <if test="bean.email != null">
                email,
            </if>
            <if test="bean.nickName != null">
                nick_name,
            </if>
            <if test="bean.sex != null">
                sex,
            </if>
            <if test="bean.password != null">
                password,
            </if>
            <if test="bean.status != null">
                status,
            </if>
            <if test="bean.createTime != null">
                create_time,
            </if>
            <if test="bean.lastLoginTime != null">
                last_login_time,
            </if>
            <if test="bean.lastOffTime != null">
                last_off_time,
            </if>
            <if test="bean.meetingNo != null">
                meeting_no,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="bean.userId != null">
                #{bean.userId},
            </if>
            <if test="bean.email != null">
                #{bean.email},
            </if>
            <if test="bean.nickName != null">
                #{bean.nickName},
            </if>
            <if test="bean.sex != null">
                #{bean.sex},
            </if>
            <if test="bean.password != null">
                #{bean.password},
            </if>
            <if test="bean.status != null">
                #{bean.status},
            </if>
            <if test="bean.createTime != null">
                #{bean.createTime},
            </if>
            <if test="bean.lastLoginTime != null">
                #{bean.lastLoginTime},
            </if>
            <if test="bean.lastOffTime != null">
                #{bean.lastOffTime},
            </if>
            <if test="bean.meetingNo != null">
                #{bean.meetingNo},
            </if>
        </trim>
        on DUPLICATE KEY UPDATE
        <trim prefix="" suffix="" suffixOverrides=",">
            <if test="bean.nickName != null">
                nick_name = VALUES(nick_name),
            </if>
            <if test="bean.sex != null">
                sex = VALUES(sex),
            </if>
            <if test="bean.password != null">
                password = VALUES(password),
            </if>
            <if test="bean.status != null">
                status = VALUES(status),
            </if>
            <if test="bean.createTime != null">
                create_time = VALUES(create_time),
            </if>
            <if test="bean.lastLoginTime != null">
                last_login_time = VALUES(last_login_time),
            </if>
            <if test="bean.lastOffTime != null">
                last_off_time = VALUES(last_off_time),
            </if>
            <if test="bean.meetingNo != null">
                meeting_no = VALUES(meeting_no),
            </if>
        </trim>
    </insert>

    <!--添加（批量插入）-->
    <insert id="insertBatch" parameterType="cn.clazs.easymeeting.entity.po.UserInfo">
        INSERT INTO user_info (user_id,email,nick_name,sex,password,status,create_time,last_login_time,last_off_time,meeting_no) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.userId},#{item.email},#{item.nickName},#{item.sex},#{item.password},#{item.status},#{item.createTime},#{item.lastLoginTime},#{item.lastOffTime},#{item.meetingNo})
        </foreach>
    </insert>

    <!--（批量插入或更新）-->
    <insert id="insertOrUpdateBatch" parameterType="cn.clazs.easymeeting.entity.po.UserInfo">
        INSERT INTO user_info (user_id,email,nick_name,sex,password,status,create_time,last_login_time,last_off_time,meeting_no) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.userId},#{item.email},#{item.nickName},#{item.sex},#{item.password},#{item.status},#{item.createTime},#{item.lastLoginTime},#{item.lastOffTime},#{item.meetingNo})
        </foreach>
        on DUPLICATE KEY UPDATE user_id = VALUES(user_id),email = VALUES(email),nick_name = VALUES(nick_name),sex = VALUES(sex),password = VALUES(password),status = VALUES(status),create_time = VALUES(create_time),last_login_time = VALUES(last_login_time),last_off_time = VALUES(last_off_time),meeting_no = VALUES(meeting_no)
    </insert>

    <!-- 根据UserId查询 -->
    <select id="selectByUserId" resultMap="base_result_map">
        SELECT <include refid="base_column_list"/> FROM user_info WHERE user_id = #{userId}
    </select>

    <!-- 根据UserId更新 -->
    <update id="updateByUserId" parameterType="cn.clazs.easymeeting.entity.po.UserInfo">
        update user_info
        <set>
            <if test="bean.userId != null">
                user_id = #{bean.userId},
            </if>
            <if test="bean.email != null">
                email = #{bean.email},
            </if>
            <if test="bean.nickName != null">
                nick_name = #{bean.nickName},
            </if>
            <if test="bean.sex != null">
                sex = #{bean.sex},
            </if>
            <if test="bean.password != null">
                password = #{bean.password},
            </if>
            <if test="bean.status != null">
                status = #{bean.status},
            </if>
            <if test="bean.createTime != null">
                create_time = #{bean.createTime},
            </if>
            <if test="bean.lastLoginTime != null">
                last_login_time = #{bean.lastLoginTime},
            </if>
            <if test="bean.lastOffTime != null">
                last_off_time = #{bean.lastOffTime},
            </if>
            <if test="bean.meetingNo != null">
                meeting_no = #{bean.meetingNo},
            </if>
        </set>
        WHERE user_id = #{userId}
    </update>

    <!-- 根据UserId删除 -->
    <delete id="deleteByUserId">
        DELETE FROM user_info WHERE user_id = #{userId}
    </delete>
    <!-- 根据Email查询 -->
    <select id="selectByEmail" resultMap="base_result_map">
        SELECT <include refid="base_column_list"/> FROM user_info WHERE email = #{email}
    </select>

    <!-- 根据Email更新 -->
    <update id="updateByEmail" parameterType="cn.clazs.easymeeting.entity.po.UserInfo">
        update user_info
        <set>
            <if test="bean.userId != null">
                user_id = #{bean.userId},
            </if>
            <if test="bean.email != null">
                email = #{bean.email},
            </if>
            <if test="bean.nickName != null">
                nick_name = #{bean.nickName},
            </if>
            <if test="bean.sex != null">
                sex = #{bean.sex},
            </if>
            <if test="bean.password != null">
                password = #{bean.password},
            </if>
            <if test="bean.status != null">
                status = #{bean.status},
            </if>
            <if test="bean.createTime != null">
                create_time = #{bean.createTime},
            </if>
            <if test="bean.lastLoginTime != null">
                last_login_time = #{bean.lastLoginTime},
            </if>
            <if test="bean.lastOffTime != null">
                last_off_time = #{bean.lastOffTime},
            </if>
            <if test="bean.meetingNo != null">
                meeting_no = #{bean.meetingNo},
            </if>
        </set>
        WHERE email = #{email}
    </update>

    <!-- 根据Email删除 -->
    <delete id="deleteByEmail">
        DELETE FROM user_info WHERE email = #{email}
    </delete>
</mapper>